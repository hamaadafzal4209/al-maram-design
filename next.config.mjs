/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.unsplash.com",
      "images.pexels.com",
      "cdn.pixabay.com",
      "source.unsplash.com",
      "plus.unsplash.com"
    ],
  },
};

export default nextConfig;
